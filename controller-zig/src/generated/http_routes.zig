const std = @import("std");

pub const Ownership = enum { head, worker, shared, proxied };
pub const Streaming = enum { never, conditional, always };

pub const Route = struct {
    method: std.http.Method,
    path: []const u8,
    ownership: Ownership,
    streaming: Streaming,
};

pub const declaration_count: usize = 96;
pub const route_count: usize = 94;

pub const routes = [_]Route{
    .{ .method = .GET, .path = "/api/docs", .ownership = .shared, .streaming = .never },
    .{ .method = .GET, .path = "/api/spec", .ownership = .shared, .streaming = .never },
    .{ .method = .POST, .path = "/benchmark", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/compat", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/compute/devices", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/compute/engines", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/compute/instances", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/compute/instances/:name/cancel", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/compute/instances/:name/stop", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/compute/launch", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/config", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/events", .ownership = .proxied, .streaming = .always },
    .{ .method = .POST, .path = "/evict", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/gpus", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/health", .ownership = .shared, .streaming = .never },
    .{ .method = .POST, .path = "/launch/:recipeId", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/launch/:recipeId/cancel", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/logs", .ownership = .proxied, .streaming = .never },
    .{ .method = .DELETE, .path = "/logs/:sessionId", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/logs/:sessionId", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/logs/:sessionId/stream", .ownership = .proxied, .streaming = .always },
    .{ .method = .GET, .path = "/peak-metrics", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/recipes", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/recipes", .ownership = .proxied, .streaming = .never },
    .{ .method = .DELETE, .path = "/recipes/:recipeId", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/recipes/:recipeId", .ownership = .proxied, .streaming = .never },
    .{ .method = .PUT, .path = "/recipes/:recipeId", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/runtime/:backend/upgrade", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/cuda", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/jobs", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/runtime/jobs", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/jobs/:jobId", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/runtime/jobs/:jobId/cancel", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/llamacpp", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/llamacpp/config", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/mlx", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/rocm", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/sglang", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/targets", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/runtime/targets/:targetId/select", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/vllm", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/runtime/vllm/config", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/status", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/diagnostics", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/downloads", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/studio/downloads", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/downloads/:downloadId", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/studio/downloads/:downloadId/cancel", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/studio/downloads/:downloadId/pause", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/studio/downloads/:downloadId/resume", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/model-index", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/model-providers", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/studio/model-providers/:providerId/login", .ownership = .head, .streaming = .never },
    .{ .method = .GET, .path = "/studio/model-providers/:providerId/login/:jobId", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/studio/model-providers/:providerId/login/:jobId/cancel", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/studio/model-providers/:providerId/login/:jobId/respond", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/studio/model-providers/:providerId/logout", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/studio/models/delete", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/studio/models/move", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/presets", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/provider-models", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/providers", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/studio/providers", .ownership = .head, .streaming = .never },
    .{ .method = .DELETE, .path = "/studio/providers/:id", .ownership = .head, .streaming = .never },
    .{ .method = .PUT, .path = "/studio/providers/:id", .ownership = .head, .streaming = .never },
    .{ .method = .GET, .path = "/studio/rigs", .ownership = .shared, .streaming = .never },
    .{ .method = .POST, .path = "/studio/rigs", .ownership = .head, .streaming = .never },
    .{ .method = .DELETE, .path = "/studio/rigs/:rigId", .ownership = .head, .streaming = .never },
    .{ .method = .PUT, .path = "/studio/rigs/:rigId", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/studio/rigs/:rigId/nodes", .ownership = .head, .streaming = .never },
    .{ .method = .DELETE, .path = "/studio/rigs/:rigId/nodes/:nodeId", .ownership = .head, .streaming = .never },
    .{ .method = .PUT, .path = "/studio/rigs/:rigId/nodes/:nodeId", .ownership = .head, .streaming = .never },
    .{ .method = .GET, .path = "/studio/sessions", .ownership = .head, .streaming = .never },
    .{ .method = .PUT, .path = "/studio/sessions/:sessionId", .ownership = .head, .streaming = .never },
    .{ .method = .GET, .path = "/studio/settings", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/studio/settings", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/studio/storage", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/studio/usage/events", .ownership = .head, .streaming = .never },
    .{ .method = .GET, .path = "/studio/usage/outbox", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/studio/usage/outbox/ack", .ownership = .head, .streaming = .never },
    .{ .method = .GET, .path = "/studio/workers", .ownership = .head, .streaming = .never },
    .{ .method = .GET, .path = "/usage", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/v1/chat/completions", .ownership = .head, .streaming = .conditional },
    .{ .method = .POST, .path = "/v1/count-tokens", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/v1/huggingface/models", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/v1/messages", .ownership = .proxied, .streaming = .conditional },
    .{ .method = .GET, .path = "/v1/metrics/vllm", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/v1/models", .ownership = .head, .streaming = .never },
    .{ .method = .GET, .path = "/v1/models/:modelId", .ownership = .head, .streaming = .never },
    .{ .method = .POST, .path = "/v1/responses", .ownership = .head, .streaming = .conditional },
    .{ .method = .GET, .path = "/v1/studio/models", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/v1/tokenize-chat-completions", .ownership = .proxied, .streaming = .never },
    .{ .method = .POST, .path = "/vram-calculator", .ownership = .proxied, .streaming = .never },
    .{ .method = .GET, .path = "/wait-ready", .ownership = .proxied, .streaming = .never },
};
